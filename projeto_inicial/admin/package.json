{
  "name": "admin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-json-server": "json-server --watch db.json",
    "start-browser-sync": "browser-sync start --server --file . --host --port 5000 --startPath /telas/lista_cliente.html",
    "start": "concurrently \"npm run start-json-server\" \"npm run start-browser-sync\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.29.3",
    "json-server": "^0.16.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
